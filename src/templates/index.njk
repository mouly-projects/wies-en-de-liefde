{% extends '../layout/default.njk' %}

{% block body %}
  <main>
    {%- component 'flyer' -%}

    <div class="page-item">
      <div class="constrain-m">
        {%- component 'entry' -%}
      </div>

      <div class="section-item">
        {%- component 'clip' -%}
      </div>

      <div class="constrain-max">
        {%- component 'entry', { type: "horizontal", "title": 'Het album' } -%}
      </div>
    </div>

    <div class="page-item">
      {%- component 'music-player' -%}
    </div>

    <div class="page-item">
      <div class="constrain-max">
        {%- component 'entry', { type: "horizontal", "title": 'Het album' } -%}
      </div>
    </div>

    <div class="area-colored">
      <div class="section-item">
        {%- component 'contact' -%}
      </div>
    </div>
  </main>
{% endblock %}
